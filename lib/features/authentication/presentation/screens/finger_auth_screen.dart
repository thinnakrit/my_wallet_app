import 'package:flutter/material.dart';

class FingerAuthScreen extends StatelessWidget {
  const FingerAuthScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('FingerAuthScreen')));
  }
}
